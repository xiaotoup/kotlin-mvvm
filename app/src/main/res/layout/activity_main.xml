<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".ui.MainActivity">

        <FrameLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <LinearLayout
            android:id="@+id/llTab"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@color/white"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tabHome"
                style="@style/tab_text_style"
                android:drawableTop="@drawable/tab_home_check_sel"
                android:text="首页" />

            <TextView
                android:id="@+id/tabFound"
                style="@style/tab_text_style"
                android:drawableTop="@drawable/tab_found_check_sel"
                android:text="发现" />

            <TextView
                android:id="@+id/tabNew"
                style="@style/tab_text_style"
                android:drawableTop="@drawable/tab_new_check_sel"
                android:text="消息" />

            <TextView
                android:id="@+id/tabMine"
                style="@style/tab_text_style"
                android:drawableTop="@drawable/tab_mine_check_sel"
                android:text="我的" />
        </LinearLayout>
    </LinearLayout>
</layout>